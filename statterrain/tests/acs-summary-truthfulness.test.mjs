import test from 'node:test';import assert from 'node:assert/strict';import { truthfulSummary } from '../scripts/public-data/acs-workflow-gates.mjs';
test('stale national manifest cannot create current-run success and metadata failure is truthful',()=>{const status={requestedRelease:'2024',buildMode:'national',targetBranch:'data',metadataValidationStatus:'FAIL',probeStatus:'NOT RUN',buildStatus:'NOT RUN',artifactValidationStatus:'NOT RUN',commitEligible:false,completionDeclaration:'BLOCKED — ACS VARIABLE METADATA VALIDATION FAILED'}; const text=truthfulSummary({status,manifest:null}); assert.match(text,/Metadata validation: FAIL/); assert.match(text,/Probe: NOT RUN/); assert.match(text,/County records: not generated/); assert.doesNotMatch(text,/Variable metadata validation: PASS/);});

test('promoted and commit-gated status is not reported as provisional',()=>{const status={requestedRelease:'2024',buildMode:'national',targetBranch:'data',metadataValidationStatus:'PASS',probeStatus:'PASS',buildStatus:'PASS',artifactValidationStatus:'PASS',promotionStatus:'PASS',commitGateStatus:'PASS',commitEligible:true,completionDeclaration:'VALIDATED — NATIONAL ACS BUILD COMPLETE, GENERATED-DATA COMMIT PENDING'}; const text=truthfulSummary({status,manifest:{countyRecordCount:3222,partitions:{A:{}},runSummary:{statesCompleted:['A'],statesFailed:[],uniqueGeoids:3222,duplicateGeoids:0}},provisional:false}); assert.doesNotMatch(text,/PROVISIONAL — NOT PROMOTED OR COMMITTED/); assert.match(text,/VALIDATED — NATIONAL ACS BUILD COMPLETE, GENERATED-DATA COMMIT PENDING/);});
