import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('summary panel shows compact approved sections without unsupported boxes',()=>{const s=readFileSync('src/components/regional-summary/RegionalSummaryPanel.tsx','utf8'); for (const t of ['Area Summary','Total hospitals','Total population','Below poverty','Limited-English-speaking households','Research prototype']) assert.match(s,new RegExp(t)); assert.doesNotMatch(s,/Critical access hospitals|Pharmacies|Dialysis facilities|Planning considerations/);});
