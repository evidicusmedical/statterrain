import test from 'node:test';import assert from 'node:assert/strict';import { spawnSync } from 'node:child_process';import { readFileSync } from 'node:fs';
test('fixture generated partitions validate checksums totals and security',()=>{spawnSync('node',['scripts/public-data/pull-acs-county-population.mjs','--fixture'],{encoding:'utf8'});const r=spawnSync('node',['scripts/public-data/validate-acs-county-population.mjs','--fixture'],{encoding:'utf8'});assert.equal(r.status,0,r.stderr);const report=JSON.parse(readFileSync('data/reports/acs-county-validation-v0.3.6.json','utf8'));assert.equal(report.status,'PASS');});
