import test from 'node:test';import assert from 'node:assert/strict';import { ACS_COUNTY_METRICS } from '../scripts/public-data/acs-county-metric-registry.mjs';
test('eight approved inactive metrics have definitions universes and variables',()=>{assert.equal(ACS_COUNTY_METRICS.length,8);for(const m of ACS_COUNTY_METRICS){assert.ok(m.definition);assert.ok(m.universe);assert.ok(m.estimateVariables.length);assert.ok(m.moeVariables.length);assert.equal(m.safeToDisplay,false);assert.deepEqual(m.availableVisualizationModes,[]);assert.ok('numeratorEstimateVariables'in m);assert.ok('denominatorEstimateVariables'in m);}});
