import test from 'node:test';import assert from 'node:assert/strict';import { spawnSync } from 'node:child_process';import { readFileSync } from 'node:fs';
test('failure report and console name exact variables and rules without keyed URLs',()=>{const r=spawnSync('node',['--input-type=module','-e',`import {validateVariable,RULES} from './scripts/public-data/validate-acs-variable-metadata.mjs'; const metric={metricId:'total_population',displayLabel:'Total population',acsGroup:'BAD',estimateVariables:['B01003_001E'],moeVariables:['B01003_001M'],numeratorEstimateVariables:[],numeratorMoeVariables:[],denominatorEstimateVariables:[],denominatorMoeVariables:[],unit:'count',universe:'Total population',datasetFamily:'acs/acs5'}; const variables={B01003_001E:{label:'NAME',concept:'Total population',predicateType:'string',datasetFamily:'acs/acs5'}}; const row=validateVariable({metric,variableId:'B01003_001E',variables,groups:{}}); console.error(JSON.stringify(row)); process.exit(row.failedRules.includes(RULES.groupMismatch)?0:1);`],{encoding:'utf8'});assert.equal(r.status,0,r.stderr);assert.match(r.stderr,/B01003_001E/);assert.match(r.stderr,/ACS-META-003/);assert.doesNotMatch(r.stderr,/key=/);});
test('workflow summary and upload paths include v0.3.6.3 diagnostics',()=>{const wf=readFileSync('../.github/workflows/acs-county-population-national-build.yml','utf8');assert.match(wf,/if: always\(\)/);assert.match(wf,/acs-county-variable-validation-v0\.3\.6\.3\.json/);assert.match(wf,/acs-variable-metadata-regression-v0\.3\.6\.3\.json/);assert.match(wf,/acs-county-current-run-status\.json/);});
