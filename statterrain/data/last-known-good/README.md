# Last-Known-Good Public Data

Future behavior: if validation fails, generated data are not published and an explicitly labeled last-known-good dataset remains active. Refresh reports must identify fallback activation, and the UI must later surface fallback/stale status.

v0.2.0 does not contain real last-known-good data.
