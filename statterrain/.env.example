# StatTerrain v0.1.0 requires NO environment variables.
#
# This file is intentionally present (but empty of required values) to
# document that fact for anyone deploying the prototype. All data in this
# version is static, synthetic, and bundled with the application code.
#
# If a future version adds real data sources, document required variables
# here with safe example values (never real secrets).
